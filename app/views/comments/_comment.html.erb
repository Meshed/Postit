<%= div_for comment do %>
	<span class="dateandoptions">
		Posted <%= time_ago_in_words(comment.created_at)%> ago<br />
	</span>

	<p><b><%= comment.user.name %></b> wrote: </p>
	<br />
	<%= content_tag(:p, comment.comment, :class => "comment-body") %>
<% end %>