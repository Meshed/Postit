  		<div class="post">
        <div class="description">
          <%= post.description %>
        </div>
        <div class="url">
    		  <%= link_to post.url, "http://" + post.url %>
        </div>
    		Poster: <%= link_to post.user.name, user_path(post.user_id) %>
        Comments: <%= link_to post.comments.count, post_path(post.user_id) %>
        <div class="view">
          <%= link_to 'View', post %>
        </div>
      </div>

