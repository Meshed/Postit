  		<div class="post">
        <div class="url">
    		  <%= link_to post.description, post.url %>
        </div>
        <h3>
      		<%= link_to "Poster: " + post.user.name, user_path(post.user_id) %>
          <%= link_to "Comments: #{post.comments.count}", post_path(post.user_id) %>
        </h3>
      </div>

